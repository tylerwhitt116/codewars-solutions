function sumStr(a,b) {
// a and b are strings of a number
  // convert a and b to actual numbers
  //create the equation/sum of the two numbers
  //return the sum as a string
  
  
  let int1 = Number(a);
  let int2 = Number(b);
  
  
  let sum = int1 + int2;
  
  return sum.toString();
  
  
  
  
}